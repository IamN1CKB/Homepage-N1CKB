#include <stdio.h>

int main()
{
    printf("Hallo world!\n");
    // questo è un commento monoriga
// \n è un carattere speciale che manda a capo

// sviluppato in AT&T, per il sistema operativo Unix.
    int a,b,r;
    a = 5;
    b = 5;
    r = a+b;   //commento a caso 
    printf("I l risultato è %d \n", r);
    /*questo è 
    un commento 
    multiriga*/
    // non si possono inserire commenti nei commenti.


    //una variabile di tipo char è grande un byte, e usa i caratteri ASCII
    // char b;
    // b = 'b';
    // printf(b);

     
}